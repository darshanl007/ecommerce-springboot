spring.application.name=ecommerce

# =========================
# SERVER
# =========================
server.port=8080

# =========================
# RAILWAY MYSQL (INTERNAL)
# =========================
# datasource settings – use Railway environment variables where possible
# the internal hostname (mysql.railway.internal) is only reachable from within the
# Railway network; if you are running the app locally use the public proxy URL or
# override with the JDBC_DATABASE_URL env var.

spring.datasource.url=${JDBC_DATABASE_URL:jdbc:mysql://shinkansen.proxy.rlwy.net:14234/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC}
spring.datasource.username=${MYSQLUSER:root}
# NOTE: the password in the original file had an incorrect case for the 'f'.
spring.datasource.password=${MYSQLPASSWORD:BakptMCEfAdzbWkDanPTLFRkDJuaSvwN}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =========================
# JPA
# =========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# explicit dialect is not required and triggers a warning; Hibernate will
# determine the correct one from the JDBC metadata.  You can drop this line.
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# =========================
# THYMELEAF
# =========================
spring.thymeleaf.cache=false

#Connection Stability (Important for Railway)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.initialization-fail-timeout=0

#Email Properties
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=darshanl1254@gmail.com
spring.mail.password=aygc oozo prdg dtxp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

#Cloudinary Properties
cloudinary.cloud_name=dlkzkocq9
cloudinary.key=445575721697743
cloudinary.secret=zIGXFGZhICm4CPWx8wLWff0HL88

#Razorpay Properties
razorpay.key=rzp_test_f4vcAPoh0RDZfi
razorpay.secret=jjblWSJ6F7NJuPUOtNmDjg4i

#Admin Credentials
admin.email=admin
admin.password=admin

#For redirect to work 
server.forward-headers-strategy=FRAMEWORK

# disable bean validation on JPA lifecycle (use jakarta namespace)
spring.jpa.properties.hibernate.validator.apply_to_ddl=false
# or, equivalently:
# spring.jpa.properties.jakarta.persistence.validation.mode=none